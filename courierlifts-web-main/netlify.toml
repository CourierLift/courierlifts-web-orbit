[build]
  publish   = "."
  functions = "netlify/functions"

# Send /api/* to our Netlify Function (which then calls Render)
[[redirects]]
  from = "/api/*"
  to   = "/.netlify/functions/proxy/:splat"
  status = 200
  force  = true
